// const express = require("express");
// const app = express();
// app.use(express.json());
// app.use(express.urlencoded({ extended: true }));
// app.use("/baku", require("./CommentBox.js "));

// app.get("/baku", function (req, res) {
//   res.json(phonebooks);
// });

// app.post("/baku", function (req, res) {
//   req.params.id;
//   phonebooks.push({ ...req.body, id: Date.now() });
//   res.json(req.body);
// });

// app.listen(3050, () => console.log(`App listening at port 3050`));
